import React from "react";
import { HeaderContent, StyledHeader } from "./Header.styles";

function Header(): JSX.Element {
  return (
    <StyledHeader>
      <HeaderContent>Oddschecker</HeaderContent>
    </StyledHeader>
  );
}

export default Header;
